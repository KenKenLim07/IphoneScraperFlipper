# TODO — Sold/Unavailable Listings Handling

- Decide default public view behavior (active only vs all statuses).
- Add a status toggle preset for public listings (active/sold/unavailable).
- Decide whether to archive old sold/unavailable (e.g., after 30 days).
- Add optional “hide sold/unavailable” switch in UI filters.
- Consider a lightweight archive table or soft-hide flag.
- Update monitor schedule if we want periodic “sold confirmation” checks.
- Add documentation for status lifecycle and cleanup policy.
